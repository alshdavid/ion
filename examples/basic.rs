pub fn main() {
  // let mut ctx = ion::Context::new();

  // ctx.exec_blocking(move |mut env| {
  //   let code = v8::String::new(&mut env.context_scope, "'Hello' + ' World!'").unwrap();

  //   let script = v8::Script::compile(&mut env.context_scope, code, None).unwrap();
  //   let result = script.run(&mut env.context_scope).unwrap();

  //   let result = result.to_string(&mut env.context_scope).unwrap();
  //   println!("{}", result.to_rust_string_lossy(&mut env.context_scope));
  // });
}
